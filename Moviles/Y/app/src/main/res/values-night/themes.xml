<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Y" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Branding -->
        <item name="colorPrimary">@color/twitterBlue</item>
        <item name="colorSecondary">@color/twitterBlue</item>
        <item name="colorPrimaryContainer">@color/twitterBlue</item>

        <!-- Fondo-->
        <item name="colorSurface">@color/twitterBlack</item>
        <item name="android:colorBackground">@color/colorBlack</item>
        <item name="android:windowBackground">@color/colorBlack</item>

        <!-- Texto -->
        <item name="colorOnPrimary">@color/colorWhite</item>
        <item name="colorOnSurface">@color/twitterBlue</item>
        <item name="colorOnBackground">@color/twitterBlue</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/colorBlack</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:fontFamily">sans-serif</item>

    </style>
</resources>